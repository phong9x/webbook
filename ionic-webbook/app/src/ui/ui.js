'use strict';

/**
 * @ngdoc overview
 * @name IonicWebbook
 * @description
 * # UI stuffs
 *
 * 
 */

function scrollTop($) {
	// alert($(document).width());
}

module.exports = function($) {
	// scrollTop($);

	// setTimeout(function(){
	// 	$("[name=viewport]").attr( 'content', 'width=640, user-scalable=no' );
	// }, 2000);
};